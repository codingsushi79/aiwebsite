# Required: password that unlocks the app (login page). Not your Ollama password.
SITE_PASSWORD=your_secret_password

# Optional: separate secret for signing session cookies (defaults to SITE_PASSWORD).
# SESSION_SECRET=

# Optional Ollama:
# OLLAMA_URL=http://127.0.0.1:11434
# OLLAMA_MODEL=llama3.1